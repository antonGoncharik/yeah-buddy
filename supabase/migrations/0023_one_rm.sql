-- =========================================
-- Максимум на один раз (1ПМ) у упражнения.
-- Нужен для силовых таблиц, где проценты считаются от соревновательного
-- максимума, а не от рабочего веса. Пусто — считаем от рабочего веса.
-- =========================================

alter table public.exercises
  add column if not exists one_rm numeric check (one_rm is null or one_rm > 0);
